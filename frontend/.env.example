# ClubOps AI Environment Configuration

# API Mode: 'mock' for local in-memory mock data, or 'api' for live REST backend
VITE_API_MODE=mock

# Base URL for the backend API when VITE_API_MODE=api
VITE_API_BASE_URL=http://localhost:8000/api
