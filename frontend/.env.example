# ClubOps AI - Environment Configuration

# API Mode: 'mock' (local development with built-in mock data) or 'api' (live backend)
VITE_API_MODE=mock

# Base URL for API endpoints when VITE_API_MODE=api
VITE_API_BASE_URL=/api
