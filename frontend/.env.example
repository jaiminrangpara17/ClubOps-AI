# API Mode: "mock" (default, runs frontend independently with mock data) or "api" (connects to backend API)
VITE_API_MODE=mock

# Backend API Base URL (used when VITE_API_MODE=api)
VITE_API_BASE_URL=http://localhost:8000/api
